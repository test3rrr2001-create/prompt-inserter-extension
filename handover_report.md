# スプレッドシート連携・同期仕様書 (Codex引継ぎ用)

このドキュメントは、Googleスプレッドシート上のプロンプト依頼を、既存のカテゴリ体系を維持したまま拡張機能へ同期するための技術仕様をまとめたものです。

## 1. 連携先スプレッドシート
- **URL**: [プロンプト管理マスタ](https://docs.google.com/spreadsheets/d/1xKQCqHPx2EWLs5WvLIiyB_RP7qU0Wv4-os_ZRc5dI_E/edit?gid=1506509549)
- **対象シート**: 「追加依頼」 (gid=1506509549)

## 2. 同期対象の抽出条件
以下の条件に合致する行を抽出し、拡張機能の `prompts.md` および `prompt_meta.json` に反映します。

- **フィルタ要素**: G列（状態）
- **抽出キーワード**: 「未処理」
- **使用カラム**:
  - B列: タイトル (ID生成および `prompt_meta.json` の `fullTitle`)
  - C列: カテゴリ (マッピングに使用)
  - D列: 本文 (`prompts.md` のコードブロック内)
  - E列: 補足 (`prompt_meta.json` の `description`)

## 3. カテゴリマッピング定義
スプレッドシート側の新しいカテゴリ分類を、拡張機能側の**以前のカテゴリ体系**に以下のように読み替えて取り込みます。このマッピングを崩さないことが重要です。

| スプレッドシート側 (Source) | 拡張機能側 (Target/Old) |
| :--- | :--- |
| 営業 | 商社・営業向け |
| 業務効率化 / 全社共通 | 全社共通 |
| マーケティング | マーケティング・SNS向け |
| 商品企画 / デザイン | 商品企画・デザイン向け |
| 開発 | 開発・実装向け |
| 資料作成 / Deep Research | Deep Research |

## 4. 更新ワークフロー
1. スプレッドシートから「未処理」行を取得（ブラウザツール等を使用）。
2. 新規ID（`prompt_xxxx`）をランダム生成。
3. `prompt_meta.json` の末尾にオブジェクトを追加（マッピング後のカテゴリを適用）。
4. `prompts.md` の末尾にMarkdown形式のセクションとプロンプト本文を追加。
5. `manifest.json` のバージョン（patch）をインクリメント。
6. `CHANGELOG.md` に更新内容を追記。
7. GitHubの `main` ブランチへプッシュ。
